import { LearnLayout } from "@/components/learn/LearnLayout";

export default function LearnRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}

/** Lesson pages use LearnLayout directly with lessonMode */
